# [Claude Code 작업 지시서] PCT(부품원가계산서) 작업 관리 대시보드 및 DB 관리

- 기획자: 한지혜 (HD건설기계, 원가기획)
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] — 수강생 프로젝트 기획서 12번
- 프로젝트 유형: 업무 워크플로 웹 시스템 (요청 → 수행 → 산출물 제출 → 검토/수정 → 완료)
- 대체 대상: **Teams 사내 공유 폴더의 구글 엑셀 공유 문서** + 각자 흩어진 개인 폴더

---

## 0. Claude Code에게 — 작업 방식

1. 이 기획서는 **이미 한 번 시제품을 만들어 본 뒤에 쓴 것**이다("과제 기획한 내용을
   간단히 바이브 코딩해 보았습니다"). 화면 시안이 함께 왔으므로 **화면 구조는 시안을 따르고**,
   시안이 다루지 않은 규칙을 찾아 채우는 것이 이 작업의 몫이다.
2. 진짜 문제는 화면이 없다는 것이 아니라 **공유 엑셀에는 규칙이 없다**는 것이다.
   - '요청'이던 줄이 다음 주에 '완료'로 바뀌어 있고 그 사이가 남지 않는다.
   - 파일이 없는데 "제출 완료"로 적혀 있다.
   - 요청자의 수정본이 수행자의 초안을 덮어써 원본을 잃는다.
   - 엑셀 열이 밀려 희망완료일이 요청일보다 빠른 줄이 생긴다.
   **이 네 가지를 규칙으로 못 박는 것**이 최우선이다. 화면은 그다음이다.
3. 규칙은 `js/logic.js`(UMD)에 모아 Node 단위 테스트로 고정하고,
   **같은 규칙을 `supabase/schema.sql` 의 제약·트리거·RLS 로 한 번 더** 적는다.
   브라우저 코드만 믿으면 REST 를 직접 부르는 순간 전부 통과한다.
4. **사내 실데이터를 저장소에 넣지 않는다.** 품번 체계·모델명·업무 흐름 같은 구조는 살리고,
   값은 전부 `scripts/gen-seed.py` 가 만든 샘플을 쓴다.
5. 기획서 원문(부록 A)이 최종 기준이다.

---

## 1. 한 줄 요약

부품원가계산서(PCT) 작업의 **요청 → 수행 → 산출물 제출 → 검토/수정 → 완료** 전 과정을
하나의 웹 시스템으로 표준화하고, 개인 폴더와 공유 폴더에 흩어져 있던 **옛 산출물을
파일명만 보고 일자·품번으로 정리**하는 업무 시스템.

## 2. 배경 및 현재 업무 상황

- PCT 는 개발 기종의 **부품마다 표준원가를 산출하는 엑셀 시트**다.
- 작업은 두 사람이 나눠 한다.
  - **작업 요청자** — 당팀 팀원. 품번을 정해 요청하고, 결과를 검토·수정해 확정한다.
  - **작업 수행자** — 별도의 **외부 작업자**. 기초 작업(초기 산출)을 수행한다.
- 현재 관리 방식
  - 외부 작업자 작업 관리 → Teams 사내 공유 폴더의 **구글 엑셀 공유 문서**
  - 외부 작업자의 초기 산출물 → Teams 공유 폴더의 **개발 모델 폴더에 품번별로** 정리
- 문제
  - 초기 부품원가계산서는 **그대로 쓸 수 없고 요청자가 일부 수정해서** 쓴다.
  - 그 수정본이 요청자 개인 폴더나 공유 폴더 여기저기에 **산재**해 관리가 어렵다.
  - 결과적으로 "이 품번의 최신본이 무엇인가"를 아무도 확실히 답하지 못한다.

## 3. 요구 기능 (기획서 6개 항목)

### 3.1 대시보드
- KPI 카드 4장: 총 산출물 완료 수 / 진행 건수 / 오늘 완료 예정 / 산출물 총 개수
- 상태별 도넛 차트, 월별 완료 추이 라인 차트
- 업무 프로세스 5단계 다이어그램, 빠른 작업 바로가기

### 3.2 작업 요청 관리
- 신규 요청 등록: 품번, 품명, 모델명, 요청일, 희망완료일, 요청자 메일
- 상태 태그(요청 / 작업중 / 검토·수정 / 완료), 검색 필터

### 3.3 작업 수행 관리
- 담당자가 **예상 완료일과 진행 상태를 직접 입력**
- **품번 1건당 다중 파일 업로드**
- 산출물 완료 메일 발송 연동

### 3.4 산출물 관리
- 요청 품번별 산출물 파일 목록화 및 다운로드

### 3.5 검토/수정 관리 — **작업 요청자만 접근**
- 요청자의 수정본 업로드와 **버전별 이력 표시**
- Database 관리: 개인이 갖고 있거나 사내에 숨어 있던 기존 자료 개별 등록.
  기획서 원문: *"AI가 파일명만 보고 찾아서 알아서 넣을 수 있는지…
  (EX. 부품원가계산서_20150103 으로 저장되어 있기 때문에 일자별로 찾기도 가능할 것 같음)"*

### 3.6 사용자 권한 관리
- 작업 수행자는 **외부 인원**이므로 회원가입 시 권한 부여 여부가 필요

## 4. 기획자가 걱정한 두 가지

> "약간의 용량 문제와 사내 팀즈 연동 시 권한 문제 등이 예상될 것 같은데
> 어떻게 해결해야할 지가 약간 막막한 부분이긴 합니다."

이 두 문장이 이 프로젝트에서 가장 중요한 요구사항이다. 답을 코드로 낸다.

- **용량** — 파일 본문을 시스템에 넣지 않는다. 표에는 이름·크기·버전·올린 사람과
  원본 위치(link)만 남기고, 실물은 지금 있는 자리(Teams/SharePoint 또는 Storage)에 둔다.
  부품원가계산서 한 건이 수 MB 이고 품번마다 여러 버전이 쌓이므로, 파일을 끌어안는 설계는
  브라우저든 무료 DB 든 몇 달 안에 반드시 막힌다.
- **권한** — Teams 연동을 전제로 만들지 않는다. 사내 승인이 나기 전에도 오늘 쓸 수 있어야 하고,
  나중에 연동이 열리면 그때 링크만 바꿔 끼울 수 있어야 한다. 메일도 같은 이유로
  시스템이 직접 보내지 않고 `mailto:` 로 메일 클라이언트를 열어 사람이 확인 후 보낸다.

## 5. 반드시 규칙으로 막을 것 (테스트로 고정)

| # | 규칙 | 어디서 막는가 |
|---|---|---|
| 1 | 상태는 한 칸씩만. 요청 → 완료로 건너뛸 수 없다 | `Logic.canTransition` + `enforce_status_flow` 트리거 |
| 2 | 산출물이 0건이면 검토로 넘어갈 수 없다 | 같음 |
| 3 | 되돌릴 때는 사유가 필요하다 | 같음 |
| 4 | 수정본은 초안을 덮어쓰지 않는다 (종류별 버전) | `Logic.nextVersion` + `deliverable` 유니크 제약 |
| 5 | 희망완료일 ≥ 요청일 | `Logic.validateRequest` + `check` 제약 |
| 6 | 외부 수행자는 배정된 건만 본다 | `Logic.visibleRequests` + `request_read` RLS |
| 7 | 검토/수정은 요청자만 | `Logic.canReview` + `deliverable_insert` RLS |
| 8 | 승인 전 계정은 아무것도 못 한다 | `Logic.isApproved` + `my_role()` |
| 9 | 파일명에서 못 읽은 값을 지어내지 않는다 | `Logic.parseArchiveName` + `archive_confirmed_needs_key` |

## 6. 산출물

- 빌드 없이 도는 순수 HTML/CSS/JS (GitHub Pages 배포)
- 데모 모드(localStorage) / Supabase 모드 — 화면과 규칙은 같고 저장 위치만 다르다
- `test/*.test.js` (Node), `scripts/sqltest/run.sh` (임시 PostgreSQL 에 스키마를 실제 적용)
- `README.md`, `docs/개발일지.md`

---

## 부록 A. 기획서 원문

> **PCT(부품원가계산서) 작업 관리 대시보드 구축 및 DB 관리**
>
> PCT(부품원가계산서)의 경우 개발 기종의 각 부품마다의 표준원가를 산출하는 엑셀시트이며,
> 해당 작업은 작업 요청자(당팀의 팀원=작업 요청자)이 별도의 외부 작업자(작업 수행자)가
> 기초 작업을 수행합니다.
>
> 외부 작업자의 작업 관리는 기존에 팀즈의 사내 공유 폴더의 구글 엑셀의 공유 문서를 통해
> 관리가 되었으며, 외부 작업자가 산출한 초기 부품원가계산서의 경우 팀즈 사내 공유 폴더에
> 개발 모델 폴더에서 품번별로 정리가 되어 있습니다.
>
> 다만, 초기 부품원가계산서는 그대로 사용할 수 없고 작업 요청자가 일부 수정해서 사용해야
> 하다보니 작업 요청자의 개별 폴더나 팀즈 공유 폴더 내에 산재되어 있어 관리가 어렵습니다.
>
> 따라서, 진행 프로젝트는 부품원가계산서 작업의 "요청 → 수행 → 산출물 제출 → 검토/수정 →
> 완료"에 이르는 전 과정을 하나의 웹 시스템으로 표준화하여, 담당자별 업무 부담을 줄이고
> 작업 이력과 파일을 체계적으로 관리하고자 합니다.
>
> \* 과제 기획한 내용을 간단히 바이브 코딩해 보았습니다.(상기의 링크 이미지 참고)
>
> **[주요 개발 요소]**
>
> 1. 대시보드
>    - 전체 현황 요약
>    - 진행률 시각화
>      * KPI 카드(총 산출물 완료 수 /진행 건수/오늘 완료 예정/ 산출물 총 개수)
>      * 상태별 도넛 차트, 월별 완료 추이 라인차트
>      * 업무 프로세스 5단계 다이어그램, 빠른 작업 바로가기
> 2. 작업 요청 관리
>    - 요청 등록/조회
>    - 검색 필터
>      * 신규 작업 요청 등록(품번, 품명, 모델명, 요청일, 희망완료일, 요청자 메일)
>      * 요청 상태 태그 관리(요청, 작업중, 검토, 완료) 태그 관리
> 3. 작업 수행 관리
>    - 진행 상태 갱신 및 일자 조정
>    - 산출물 업로드 및 자동 메일 발송
>      * 담당자의 예상 완료일, 진행 상태 직접 입력
>      * 품번 1건당 다중 파일 업로드
>      * 산출물 완료 메일 발송 연동
> 4. 산출물 관리
>    - 산출물 보관/이력화
>      * 요청 품번별 산출물 파일 목록화 및 다운로드
> 5. 검토/수정 관리(작업 요청자만 접근 가능)
>    - 요청자 최종 검토
>    - 수정 이력 관리
>    - Database 관리
>      * 작업 요청자의 수정본 업로드 및 버전별 이력 표시
>      * 기존 개인이 갖고 있던 or 사내에 숨겨져 있던 기존 부품원가계산서 자료 개별 등록
>        -> AI가 파일명만 보고 찾아서 알아서 넣을 수 있는지..
>        (EX. 부품원가계산서_20150103 으로 저장되어 있기 때문에 일자별로 찾기도 가능할 것 같음)
> 6. 사용자 권한 관리
>    - 작업 수행자는 외부 인원으로 회원가입 시 권한 부여 여부 필요
>
> 약간의 용량 문제와 사내 팀즈 연동 시 권한 문제 등이 예상될 것 같은데
> 어떻게 해결해야할 지가 약간 막막한 부분이긴 합니다.

## 부록 B. 화면 시안에서 읽은 것

기획자가 함께 보낸 시안(대시보드 한 장)에서 그대로 가져온 것:

- 좌측 짙은 남색 고정 사이드바 + 우측 밝은 본문
- 메뉴 묶음: MAIN(대시보드) / 작업 관리 / 파일 관리 / 사용자·권한 관리
- KPI 카드 4장 → 도넛 + 라인 + 오늘의 일정 3단 → 최근 요청 목록 + 오늘 완료 예정 → 빠른 작업
- 품번 표기 `123456-100001`, 요청번호 `REQ-001`, 상태 태그 4종

시안이 정하지 않아 이 작업에서 채운 것:

- 상태 전이 규칙과 되돌리기, 산출물 버전 체계, 권한 경계, 파일명 해독 규칙,
  용량·권한 문제에 대한 설계상의 답(§4)
